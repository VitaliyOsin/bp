export const cel = (el) => document.createElement(el);
export const gel = (el) => document.getElementById(el);
export const qel = (el) => document.querySelector(el);
export const qell = (el) => document.querySelectorAll(el);
